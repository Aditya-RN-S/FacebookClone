<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/seach_menu"
        android:title="@string/messenger_icon"
        android:icon="@drawable/ic_searchdp"
        android:iconTint="@color/fb_text_black"
        app:showAsAction="always"/>

    <item android:id="@+id/messenger_menu"
        android:title="@string/search_icon"
        android:icon="@drawable/ic_messenger"
        android:iconTint="@color/fb_text_black"
        app:showAsAction="ifRoom"/>
</menu>